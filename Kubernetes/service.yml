apiVersion: v1
kind: Service
metadata:
  name: footstream-app
  labels:
    app: footstream-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: footstream-app
